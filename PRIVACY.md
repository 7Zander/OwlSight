# 隐私说明

OwlSight 使用本地界面与随包运行资源，不提供遥测、自动更新或自动上传日志功能。用户打开网络盘上的 EXR、OCIO 配置或 LUT 时，会产生相应的文件读取。

用户设置与 Chromium 本地数据保存在 `%APPDATA%/OwlSight-Prototype`，该路径为兼容已有配置而保留。性能日志默认开启，写入 EXE 同目录 `logs`，设置中可关闭。每次启动生成新文件，单文件最多 8 MiB；旧日志不自动清理。

日志可能包含软件版本、硬件及进程信息、EXR / OCIO 路径、性能计时和错误。用户主动分享日志前，应检查并移除不希望公开的用户名、项目名称和目录信息。

“设为 EXR 默认程序”只在用户点击后登记当前程序并打开 Windows 默认应用设置，由用户在系统中确认。不会在启动或解压时更改默认程序。
